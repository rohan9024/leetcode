        // // If not found
        // auto it = find_if(nums.begin(), nums.end(), [target](int num)
        //                   { return num > target; });

        // index = distance(nums.begin(), it);